import React from 'react';
import ReactDOM from 'react-dom/client';
import PRContextWidget from './PRContextWidget';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PRContextWidget />
  </React.StrictMode>
);
